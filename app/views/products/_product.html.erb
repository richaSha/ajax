<div class="col-md-3" id="product-<%= product.id %>">
  <div class="thumbnail product-image">
    <%= image_tag product.image.url(:medium) %>
    <div class="caption">
      <%= link_to product.name, product_path(product), remote: true, class: 'static-popup-link'%>
    </div>
  </div>
</div>
